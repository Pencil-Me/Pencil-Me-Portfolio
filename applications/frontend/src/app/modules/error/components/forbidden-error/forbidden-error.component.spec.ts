import { ForbiddenErrorComponent } from './forbidden-error.component';

describe('ForbiddenErrorComponent', () => {
  let component: ForbiddenErrorComponent;

  beforeEach(() => {
    component = new ForbiddenErrorComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
